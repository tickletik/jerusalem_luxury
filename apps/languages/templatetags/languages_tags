from django import template

register = template.Library()


@register.inclusion_tag('jlinx_utils/test.tmpl')
def show_titledesc(formset, label):
    return {'FORMSET':formset, 'LABEL':label }
         
