<image_list>
    {% for image in imagelist %}
    <image>
        <url>{{MEDIA_URL}}/{{image.slideshow}}</url>
        <caption>{{image.description}}</caption>
    </image>
    {% endfor %}
</image_list>
